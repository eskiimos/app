# app

Telegram bot for generating commercial offers based on the Total Lookas catalog.

## Setup

Create a virtual environment, install requirements and run the bot:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python bot.py
```

Replace `YOUR_TOKEN_HERE` in `bot.py` with your actual Telegram bot token.
